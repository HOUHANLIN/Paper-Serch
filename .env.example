# 可选：Web 应用与鉴权配置
# SECRET_KEY 用于 Flask session 签名；生产环境务必设置为随机长字符串
SECRET_KEY=
# SQLite 数据库路径（留空默认 paper_serch.db）
PAPER_SERCH_DB_PATH=
# 注册默认赠送的工作流次数（默认 10）
INITIAL_CREDITS=10
# 是否允许自由注册（默认关闭）
ALLOW_SELF_REGISTRATION=false
# 可选：启动时自动创建的管理员账号（存在则跳过）
ADMIN_EMAIL=
ADMIN_PASSWORD=

# AI 提供方预设（未登录或普通用户使用预设，管理员可修改）
PRESET_AI_PROVIDER=openai
GEMINI_API_KEY=
GEMINI_MODEL=
GEMINI_TEMPERATURE=0

OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=
OPENAI_TEMPERATURE=0

# 可选：PubMed 请求配置（影响稳定性与速度）
# 全局默认 PubMed HTTP 并发上限（工作流页也可在单次任务内覆盖）
PUBMED_MAX_CONCURRENT_REQUESTS=3
# 失败重试次数与退避参数（429/5xx/超时/连接失败）
PUBMED_MAX_RETRIES=4
PUBMED_BACKOFF_BASE=0.6
PUBMED_BACKOFF_MAX=10

# 可选：AI 摘要并发（每篇文章一次调用；不设置/≤0 表示不限制）
AI_SUMMARY_CONCURRENCY=
