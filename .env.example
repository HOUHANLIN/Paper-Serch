# 本项目的示例配置文件。
# 使用前请复制为 ".env" 并根据需要修改为你自己的配置。

# =========================
# PubMed 检索相关配置（pubmed_bibtex.py）
# =========================

# 必填：当你直接运行 "python pubmed_bibtex.py" 且不带命令行参数时，
# 使用的 PubMed 检索式。
# 建议根据你的研究主题自行修改，例如：
# PUBMED_QUERY="artificial intelligence AND (dental implants OR implant dentistry OR oral implantology)"
PUBMED_QUERY="your_pubmed_query_here"

# 可选：检索最近多少年内的文献（默认 5 年）
PUBMED_YEARS=5

# 可选：最多返回多少条检索结果（默认 10 条）
PUBMED_MAX_RESULTS=10

# 可选：输出的 .bib 文件名称（默认 pubmed_results.bib）
PUBMED_OUTPUT="pubmed_results.bib"

# 可选：用于 NCBI E-utilities 的联系邮箱（推荐填写）
PUBMED_EMAIL="your_email@example.com"

# 可选：NCBI API Key，用于获取更高访问额度
PUBMED_API_KEY="your_ncbi_api_key_here"


# =========================
# Google Gemini / GenAI 配置（gemini_summary.py 与 pubmed_bibtex.py 中的 AI 总结）
# =========================

# 必填：调用 Gemini 模型所需的 API Key
GEMINI_API_KEY="your_gemini_api_key_here"

# 建议：默认使用的模型名称，例如：
# GEMINI_MODEL="gemini-2.5-flash"
GEMINI_MODEL="gemini-2.5-flash"

# 可选：采样温度（0 表示倾向于稳定输出）
GEMINI_TEMPERATURE=0
