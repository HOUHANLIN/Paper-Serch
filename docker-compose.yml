version: "3.9"

services:
  web:
    build: .
    container_name: pubmed-bibtex-web
    ports:
      - "5000:5000"
    # 当前版本不再依赖 .env；所有检索与 AI 配置在 Web 页面中填写。
    # 如需在容器中追加其他环境变量，可在此处配置：
    # environment:
    #   - SOME_ENV=value
    restart: unless-stopped
